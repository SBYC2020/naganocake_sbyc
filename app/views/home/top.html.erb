<div class="container">
	<div class="row">
		<div class="col-xs-2" >


			<table class="table table-bordered">
				<thead>
    				<tr>
      					<th>ジャンル検索</th>
					</tr>
				</thead>

				<tbody>
					<% @genres.each do |genre| %>
						<tr>
							<td>
								<%=link_to genre.name, products_path(genre_id: genre.id) %>
							</td>
						</tr>
					<% end %>
				</tbody>
			</table>
		</div>

		<div class="col-xs-10">
			<div class="row">
				<div class="col-xs-5 col-xs-offset-3" >
					<p>ようこそ、NaganoCakeへ！
					<br>このサイトは、ケーキ販売のECサイトになります。
					<br>会員でない方も商品の閲覧はできますが、
					<br>購入には会員登録が必要になります。</p>
				</div>
			</div>

			<div class="row" >
				<h2>オススメ商品</h2>
				<% @products.first(4).each do |p| %>
					<div class="col-xs-3" >
						<%= link_to product_path(p) do %>
　　<%= attachment_image_tag p, :image, :fill, 150, 130, size: "180x150", fallback: "no_image.png", :alt => p.product_name %>
<% end %>
						<p><%= p.product_name %></p>
						<p>¥<%= p.tax_included_price %></p>
					</div>
				<% end %>
			</div>
			<div class="col-md-8 col-xs-offset-10" >
				<%= link_to "もっと見る>",products_path %>
			</div>
		</div>
	</div>
</div>
